# Vendored Adafruit GFX subset

These files are copied from the Adafruit GFX Library version resolved by the DeskMate PlatformIO build. The native preview vendors the exact drawing implementation and classic font so desktop output uses the same primitive geometry and glyph metrics as the device.

See `license.txt` for the upstream BSD license.
