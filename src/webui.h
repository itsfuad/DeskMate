#pragma once
#include <pgmspace.h>

static const char WEBUI_HTML[] PROGMEM = R"HTML(<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>DeskMate</title><style>
:root{color-scheme:dark;--bg:#090d14;--panel:#111824;--panel2:#151f2d;--line:#263449;--text:#edf3fa;--muted:#92a2b8;--cyan:#5fd2c7;--blue:#6aa7ff;--violet:#a78bfa;--amber:#f5bd66;--rose:#fb7185;--green:#72d69b;--shadow:0 18px 60px #0008}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 15% -10%,#18263b 0,transparent 36%),var(--bg);color:var(--text);font:14px/1.45 system-ui,-apple-system,Segoe UI,sans-serif}.shell{max-width:1000px;margin:auto;padding:22px 18px 92px}header{display:flex;justify-content:space-between;align-items:center;gap:16px;margin-bottom:18px}.brand{display:flex;align-items:center;gap:12px}.mark{width:38px;height:38px;border-radius:12px;background:conic-gradient(from 210deg,var(--cyan),var(--blue),var(--violet),var(--amber),var(--cyan));box-shadow:0 0 0 5px #ffffff08}.brand h1{font-size:20px;margin:0}.brand p{margin:1px 0 0;color:var(--muted);font-size:12px}.status-dot{width:9px;height:9px;border-radius:99px;background:var(--green);box-shadow:0 0 14px var(--green)}nav{display:flex;gap:6px;overflow:auto;padding:6px;background:#0d141f;border:1px solid var(--line);border-radius:14px;margin-bottom:18px}nav button{flex:0 0 auto;border:0;background:transparent;color:var(--muted);padding:9px 13px;border-radius:9px;font-weight:650;cursor:pointer}nav button.on{background:var(--panel2);color:var(--text)}.tab{display:none}.tab.on{display:block}.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.card{background:linear-gradient(180deg,#151f2d,#101722);border:1px solid var(--line);border-radius:17px;padding:17px;box-shadow:var(--shadow)}.card.wide{grid-column:1/-1}.card h2{font-size:14px;margin:0 0 4px}.card>p{color:var(--muted);margin:0 0 15px;font-size:12px}.field{margin:12px 0}.field label{display:flex;justify-content:space-between;gap:10px;color:#c8d4e3;font-size:12px;margin-bottom:6px}.hint{color:var(--muted);font-weight:400}.row{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.row.three{grid-template-columns:repeat(3,minmax(0,1fr))}input,select{width:100%;border:1px solid #30415a;background:#0b111b;color:var(--text);border-radius:10px;padding:10px 11px;outline:none}input:focus,select:focus{border-color:var(--blue);box-shadow:0 0 0 3px #6aa7ff1f}input[type=range]{padding:0;border:0;box-shadow:none;accent-color:var(--violet)}input[type=checkbox]{width:auto;accent-color:var(--cyan)}.checks{display:flex;flex-wrap:wrap;gap:10px 16px}.checks label{display:flex;align-items:center;gap:7px;color:#cbd6e5}.btn{border:1px solid #36506f;background:#172438;color:var(--text);padding:10px 13px;border-radius:10px;font-weight:700;cursor:pointer}.btn:hover{filter:brightness(1.1)}.btn:disabled{opacity:.45;cursor:not-allowed}.btn.primary{border-color:#6aa7ff66;background:#24436f}.btn.good{border-color:#72d69b66;background:#183b2c}.btn.amber{border-color:#f5bd6666;background:#44341c}.btn.rose{border-color:#fb718566;background:#48202a}.actions{display:flex;gap:9px;flex-wrap:wrap}.validation{margin-top:10px;padding:10px 11px;border:1px solid var(--line);border-radius:11px;background:#0b111b;color:var(--muted);font-size:12px}.validation.ok{border-color:#72d69b55;color:var(--green)}.validation.err{border-color:#fb718555;color:var(--rose)}.validation.busy{border-color:#f5bd6655;color:var(--amber)}.results{display:grid;gap:7px;margin-top:9px}.result{display:flex;justify-content:space-between;text-align:left;gap:10px;padding:10px;border-radius:10px;border:1px solid var(--line);background:#0d1521;color:var(--text);cursor:pointer}.result small{display:block;color:var(--muted)}.badge{display:inline-flex;align-items:center;gap:6px;padding:4px 8px;border:1px solid var(--line);border-radius:99px;color:var(--muted);font-size:11px}.badge:before{content:"";width:6px;height:6px;border-radius:99px;background:var(--cyan)}.savebar{position:fixed;left:0;right:0;bottom:0;background:#080c12e8;backdrop-filter:blur(12px);border-top:1px solid var(--line);padding:11px 18px}.savebar-inner{max-width:964px;margin:auto;display:flex;align-items:center;gap:12px}.save-state{color:var(--muted);font-size:12px}.toast{display:none;position:fixed;right:18px;bottom:76px;max-width:340px;padding:11px 14px;border-radius:11px;background:#1a2535;border:1px solid var(--line);box-shadow:var(--shadow)}.progress{height:8px;border-radius:99px;background:#0a1018;overflow:hidden;margin:12px 0}.progress span{display:block;width:0;height:100%;background:linear-gradient(90deg,var(--blue),var(--violet),var(--cyan));transition:width .15s}.upload-status{color:var(--muted)}.upload-status.ok{color:var(--green)}.upload-status.err{color:var(--rose)}pre.status{white-space:pre-wrap;margin:0;color:#bdc9d8;font:12px/1.55 ui-monospace,SFMono-Regular,monospace}.secret{display:flex;gap:8px;align-items:center}.secret input{flex:1}.mini{font-size:11px;color:var(--muted)}@media(max-width:720px){.grid,.row,.row.three{grid-template-columns:1fr}.shell{padding:15px 12px 92px}.card.wide{grid-column:auto}header{align-items:flex-start}.status-label{display:none}}
</style></head><body><div class="shell">
<header><div class="brand"><div class="mark"></div><div><h1>DeskMate</h1><p>240 × 240 connected desk display</p></div></div><div class="badge"><span class="status-label">device online</span></div></header>
<nav><button class="on" data-tab="display">Display</button><button data-tab="weather">Weather & time</button><button data-tab="network">Network</button><button data-tab="radar">Radar</button><button data-tab="github">GitHub</button><button data-tab="system">System</button></nav>
<main>
<section id="display" class="tab on"><div class="grid">
<div class="card"><h2>Active view</h2><p>Display controls save automatically. Data sources keep independent background schedules when selected in the carousel.</p><div class="field"><label>Mode</label><select id="mode"><option value="weather">Weather</option><option value="network">Network guardian</option><option value="radar">Aircraft radar</option><option value="github">GitHub</option><option value="carousel">Carousel</option></select></div><div class="field"><label>Rotation</label><select id="rotation"><option value="0">0°</option><option value="1">90°</option><option value="2">180°</option><option value="3">270°</option></select></div><div class="field"><label>Day brightness <span id="brightnessValue">90</span>%</label><input id="brightness" type="range" min="0" max="100"></div><div id="displayAutoState" class="mini"></div></div>
<div class="card"><h2>Carousel</h2><p>Only selected screens poll. Missed deadlines coalesce into one fresh request instead of building a queue.</p><div class="checks"><label><input id="carouselWeather" type="checkbox">Weather</label><label><input id="carouselNetwork" type="checkbox">Network</label><label><input id="carouselRadar" type="checkbox">Radar</label><label><input id="carouselGithub" type="checkbox">GitHub</label></div><div class="field"><label>Dwell time <span class="hint">seconds</span></label><input id="carouselSec" type="number" min="5" max="3600"></div></div>
<div class="card wide"><h2>Time & night mode</h2><p>Uses the location resolved on the Weather tab. NTP supplies UTC; the verified location supplies the current UTC offset.</p><div class="row"><div class="field"><label>Time format</label><select id="timeFormat"><option value="24">24-hour</option><option value="12">12-hour</option></select></div></div><div class="checks"><label><input id="nightEnabled" type="checkbox">Enable scheduled dimming</label></div><div class="row three"><div class="field"><label>Starts</label><input id="nightStart" type="time"></div><div class="field"><label>Ends</label><input id="nightEnd" type="time"></div><div class="field"><label>Night brightness <span id="nightValue">0</span>%</label><input id="nightLevel" type="range" min="0" max="100"></div></div><div id="timezoneState" class="validation">No verified timezone.</div></div>
</div></section>
<section id="weather" class="tab"><div class="grid">
<div class="card wide"><h2>Resolve location in this browser</h2><p>The browser calls Open-Meteo only while configuring. DeskMate receives canonical latitude, longitude, timezone and offset, then polls OpenWeather by coordinates.</p><div class="row"><div class="field"><label>City or postal code</label><input id="weatherSearch" placeholder="Dhaka, Stockholm, 10001…"></div><div class="field" style="align-self:end"><button id="locationSearchButton" class="btn primary">Search locations</button></div></div><div id="locationResults" class="results"></div><div id="locationState" class="validation">Search and select an exact location.</div></div>
<div class="card"><h2>OpenWeather</h2><p>The key and both required endpoints are tested from your browser before they can be saved.</p><div class="field"><label>API key <span id="weatherKeyState" class="hint"></span></label><div class="secret"><input id="weatherKey" type="password" autocomplete="off" placeholder="32-character key"><button id="verifyWeatherButton" class="btn good">Verify</button></div></div><div id="weatherVerifyState" class="validation">Not verified in this session.</div></div>
<div class="card"><h2>Forecast options</h2><p>The device requests current conditions plus upcoming 3-hour points.</p><div class="row"><div class="field"><label>Units</label><select id="weatherMetric"><option value="1">Metric</option><option value="0">Imperial</option></select></div><div class="field"><label>Refresh <span class="hint">seconds</span></label><input id="weatherPoll" type="number" min="300" max="3600"></div></div></div>
</div></section>
<section id="network" class="tab"><div class="grid"><div class="card wide"><h2>Guardian targets</h2><p>Browsers cannot open raw TCP sockets, so the browser asks DeskMate to perform one bounded test before saving these values.</p><div class="row three"><div class="field"><label>TCP host</label><input id="networkHost"></div><div class="field"><label>Port</label><input id="networkPort" type="number" min="1" max="65535"></div><div class="field"><label>DNS name</label><input id="networkDns"></div></div><div class="row"><div class="field"><label>Probe interval <span class="hint">seconds</span></label><input id="networkPoll" type="number" min="3" max="300"></div><div class="field" style="align-self:end"><button id="testNetworkButton" class="btn good">Test targets</button></div></div><div id="networkTestState" class="validation">Existing configuration has not changed.</div></div></div></section>
<section id="radar" class="tab"><div class="grid">
<div class="card"><h2>Radar origin</h2><p>Aircraft positions are projected around this point.</p><div class="row"><div class="field"><label>Latitude</label><input id="radarLat" type="number" step="0.0001" min="-90" max="90"></div><div class="field"><label>Longitude</label><input id="radarLon" type="number" step="0.0001" min="-180" max="180"></div></div><button id="copyWeatherLocation" class="btn">Use weather location</button></div>
<div class="card"><h2>Data source</h2><p>Direct ADS-B or your compact webhook.</p><div class="field"><label>Source</label><select id="radarSource"><option value="direct">adsb.fi direct</option><option value="webhook">Webhook</option></select></div><div class="field"><label>Webhook URL</label><input id="radarWebhook" type="url" placeholder="https://…"></div></div>
<div class="card wide"><h2>Radar presentation</h2><div class="row three"><div class="field"><label>Range <span class="hint">km</span></label><input id="radarRange" type="number" min="1" max="500"></div><div class="field"><label>Refresh <span class="hint">seconds</span></label><input id="radarPoll" type="number" min="3"></div><div class="field"><label>Minimum altitude <span class="hint">ft</span></label><input id="radarMinAlt" type="number" min="0" max="60000"></div></div><div class="row"><div class="field"><label>Scale</label><select id="radarScale"><option value="0">Compact</option><option value="1">Balanced</option><option value="2">Large</option></select></div><div class="field"><label>Distance units</label><select id="radarMiles"><option value="0">Kilometres</option><option value="1">Miles</option></select></div></div><div class="checks"><label><input id="radarLabels" type="checkbox">Labels</label><label><input id="radarVectors" type="checkbox">Heading vectors</label><label><input id="radarRim" type="checkbox">Rim targets</label></div><div class="actions" style="margin-top:13px"><button id="testRadarButton" class="btn good">Test radar source</button></div><div id="radarTestState" class="validation">Existing configuration has not changed.</div></div>
</div></section>
<section id="github" class="tab"><div class="grid"><div class="card"><h2>User token</h2><p>The browser verifies the authenticated user and contribution query before sending the token to DeskMate.</p><div class="field"><label>Personal access token <span id="githubKeyState" class="hint"></span></label><div class="secret"><input id="githubToken" type="password" autocomplete="off"><button id="verifyGithubButton" class="btn good">Verify</button></div></div><div id="githubVerifyState" class="validation">Not verified in this session.</div></div><div class="card"><h2>Contribution graph</h2><p>Graph density adapts to the selected time range.</p><div class="field"><label>Range</label><select id="githubRange"><option value="1">1 month</option><option value="3">3 months</option><option value="6">6 months</option><option value="12">1 year</option></select></div><div class="field"><label>Refresh <span class="hint">seconds</span></label><input id="githubPoll" type="number" min="300" max="3600"></div></div></div></section>
<section id="system" class="tab"><div class="grid">
<div class="card"><h2>Wi-Fi & hostname</h2><p>Syntax is validated locally. A changed Wi-Fi identity requires reboot.</p><div class="field"><label>SSID</label><input id="ssid"></div><div class="field"><label>Password</label><input id="password" type="password" autocomplete="new-password"></div><div class="field"><label>Hostname</label><input id="hostname" maxlength="63"></div><div class="actions"><button id="scanButton" class="btn">Scan Wi-Fi</button></div><div id="wifiResults" class="results"></div></div>
<div class="card"><h2>Runtime</h2><p>Scheduler and memory telemetry.</p><pre id="status" class="status">Loading…</pre><div class="actions" style="margin-top:14px"><button id="refreshButton" class="btn">Refresh all data</button><button id="rebootButton" class="btn rose">Reboot</button></div></div>
<div class="card wide"><h2>Firmware upload</h2><p>The browser checks the image header, shows transfer progress, locks controls during upload, and waits for DeskMate to return.</p><input id="firmware" type="file" accept=".bin,application/octet-stream"><div class="progress"><span id="uploadProgress"></span></div><div id="uploadStatus" class="upload-status">Choose a .bin file to begin.</div><div class="actions" style="margin-top:11px"><button id="uploadButton" class="btn amber" disabled>Upload firmware</button></div></div>
</div></section>
</main></div>
<div class="savebar"><div class="savebar-inner"><button id="saveButton" class="btn primary">Verify & save settings</button><span id="saveState" class="save-state">Ready</span></div></div><div id="toast" class="toast"></div>
<script>
const q=id=>document.getElementById(id);let config={},uploading=false,postChain=Promise.resolve();
let selectedLocation=null,weatherVerified=false,githubVerified=false,networkVerified=true,radarVerified=true;
let initial={weather:'',network:'',radar:'',github:''};
const jsonHeaders={'Content-Type':'application/json'};
function toast(text){const el=q('toast');el.textContent=text;el.style.display='block';clearTimeout(toast.t);toast.t=setTimeout(()=>el.style.display='none',3200)}
function state(id,type,text){const el=q(id);el.className='validation '+(type||'');el.textContent=text}
function secretState(id,set){q(id).textContent=set?'configured':''}
function setSaveState(text,color=''){q('saveState').textContent=text;q('saveState').style.color=color||'';q('displayAutoState').textContent=text==='Saved automatically'?'saved':''}
async function getJson(url,options={},timeoutMs=12000){const controller=new AbortController;const timer=setTimeout(()=>controller.abort(),timeoutMs);try{const r=await fetch(url,{...options,signal:controller.signal});const text=await r.text();let data={};try{data=text?JSON.parse(text):{}}catch(e){data={error:text}}if(!r.ok)throw new Error(data.error||text||`HTTP ${r.status}`);return data}catch(e){if(e.name==='AbortError')throw new Error('request timed out');throw e}finally{clearTimeout(timer)}}
document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>{document.querySelectorAll('nav button,.tab').forEach(x=>x.classList.remove('on'));b.classList.add('on');q(b.dataset.tab).classList.add('on')});
function weatherFingerprint(){return JSON.stringify([selectedLocation?.latitude,selectedLocation?.longitude,selectedLocation?.timezone,q('weatherKey').value?'new-key':'stored'])}
function networkFingerprint(){return JSON.stringify([q('networkHost').value.trim(),q('networkPort').value,q('networkDns').value.trim()])}
function radarFingerprint(){return JSON.stringify([q('radarLat').value,q('radarLon').value,q('radarSource').value,q('radarWebhook').value.trim(),q('radarRange').value])}
function githubFingerprint(){return JSON.stringify([q('githubToken').value?'new-token':'stored'])}
function markWeatherChanged(){if(weatherFingerprint()!==initial.weather){weatherVerified=false;state('weatherVerifyState','','Location/key changes must be verified before saving.')}}
function markNetworkChanged(){if(networkFingerprint()!==initial.network){networkVerified=false;state('networkTestState','','Test the changed targets before saving.')}}
function markRadarChanged(){if(radarFingerprint()!==initial.radar){radarVerified=false;state('radarTestState','','Test the changed source before saving.')}}
function markGithubChanged(){if(q('githubToken').value){githubVerified=false;state('githubVerifyState','','Verify the new token before saving.')}}
function auto(body){postChain=postChain.then(async()=>{setSaveState('Applying…','var(--amber)');try{const r=await getJson('/api/config',{method:'POST',headers:jsonHeaders,body:JSON.stringify(body)});if(!r.ok)throw new Error(r.error||'save failed');setSaveState('Saved automatically','var(--green)');if(r.reboot)toast('Saved · rebooting')}catch(e){setSaveState('Auto-save failed','var(--rose)');toast(e.message)}});return postChain}
function clockBody(){return{clock:{use24Hour:q('timeFormat').value==='24',nightEnabled:q('nightEnabled').checked,nightStart:q('nightStart').value,nightEnd:q('nightEnd').value,nightLevel:+q('nightLevel').value}}}
function formatOffset(sec){const sign=sec>=0?'+':'-';sec=Math.abs(sec);return `UTC${sign}${String(Math.floor(sec/3600)).padStart(2,'0')}:${String(Math.floor(sec%3600/60)).padStart(2,'0')}`}
function renderLocationState(){if(!selectedLocation)return state('locationState','','Search and select an exact location.');state('locationState','ok',`${selectedLocation.name}, ${selectedLocation.country} · ${selectedLocation.latitude.toFixed(4)}, ${selectedLocation.longitude.toFixed(4)} · ${selectedLocation.timezone} · ${formatOffset(selectedLocation.utcOffsetSeconds)}`);q('timezoneState').className='validation ok';q('timezoneState').textContent=`${selectedLocation.timezone} · ${selectedLocation.timezoneAbbreviation||formatOffset(selectedLocation.utcOffsetSeconds)}`}
async function searchLocations(){const name=q('weatherSearch').value.trim();if(name.length<2)return state('locationState','err','Enter at least two characters.');state('locationState','busy','Searching Open-Meteo…');q('locationResults').innerHTML='';try{const u='https://geocoding-api.open-meteo.com/v1/search?count=7&language=en&format=json&name='+encodeURIComponent(name);const data=await getJson(u);const results=data.results||[];if(!results.length)return state('locationState','err','No matching locations.');q('locationResults').innerHTML=results.map((r,i)=>`<button class="result" data-i="${i}"><span><strong>${r.name}</strong><small>${[r.admin1,r.country].filter(Boolean).join(', ')}</small></span><small>${r.timezone||''}</small></button>`).join('');q('locationResults').querySelectorAll('button').forEach(b=>b.onclick=()=>resolveLocation(results[+b.dataset.i]));state('locationState','','Select the exact result.')}catch(e){state('locationState','err','Location lookup failed: '+e.message)}}
async function resolveLocation(r){state('locationState','busy','Resolving current timezone offset…');try{const u=`https://api.open-meteo.com/v1/forecast?latitude=${r.latitude}&longitude=${r.longitude}&timezone=auto&forecast_days=1&current=temperature_2m`;const z=await getJson(u);selectedLocation={name:r.name,country:r.country||r.country_code||'',countryCode:r.country_code||'',latitude:+r.latitude,longitude:+r.longitude,timezone:z.timezone||r.timezone||'UTC',timezoneAbbreviation:z.timezone_abbreviation||'',utcOffsetSeconds:+(z.utc_offset_seconds||0),verified:true};q('weatherSearch').value=[selectedLocation.name,selectedLocation.countryCode].filter(Boolean).join(', ');q('locationResults').innerHTML='';weatherVerified=false;renderLocationState();markWeatherChanged()}catch(e){state('locationState','err','Timezone resolution failed: '+e.message)}}
async function verifyWeather(){if(!selectedLocation)return state('weatherVerifyState','err','Select a verified location first.');const key=q('weatherKey').value.trim();if(!key)return state('weatherVerifyState','err','Re-enter the OpenWeather key to verify changes in the browser.');state('weatherVerifyState','busy','Testing current weather and forecast…');try{const units=q('weatherMetric').value==='1'?'metric':'imperial';const base=`lat=${selectedLocation.latitude}&lon=${selectedLocation.longitude}&units=${units}&appid=${encodeURIComponent(key)}`;const [current,forecast]=await Promise.all([getJson('https://api.openweathermap.org/data/2.5/weather?'+base),getJson('https://api.openweathermap.org/data/2.5/forecast?cnt=4&'+base)]);if(!current.main||!forecast.list)throw new Error('incomplete provider response');weatherVerified=true;state('weatherVerifyState','ok',`Verified ${current.name||selectedLocation.name}: ${Math.round(current.main.temp)}° · ${forecast.list.length} forecast points`)}catch(e){weatherVerified=false;state('weatherVerifyState','err','OpenWeather verification failed: '+e.message)}}
async function verifyGithub(){const token=q('githubToken').value.trim();if(!token)return state('githubVerifyState','err','Enter the token to verify it in this browser.');state('githubVerifyState','busy','Checking authenticated user and contribution access…');try{const headers={Accept:'application/vnd.github+json',Authorization:'Bearer '+token,'X-GitHub-Api-Version':'2022-11-28'};const user=await getJson('https://api.github.com/user',{headers});const months=+q('githubRange').value;const days={1:31,3:92,6:183,12:365}[months]||365;const from=new Date(Date.now()-days*86400000);const query='query($from:DateTime!,$to:DateTime!){viewer{login contributionsCollection(from:$from,to:$to){totalCommitContributions contributionCalendar{totalContributions weeks{firstDay}}}}}';const graph=await getJson('https://api.github.com/graphql',{method:'POST',headers:{...headers,'Content-Type':'application/json'},body:JSON.stringify({query,variables:{from:from.toISOString(),to:new Date().toISOString()}})});if(graph.errors?.length)throw new Error(graph.errors[0].message);const viewer=graph.data?.viewer;if(!viewer)throw new Error('GraphQL viewer unavailable');githubVerified=true;q('githubToken').dataset.login=user.login;state('githubVerifyState','ok',`Verified as @${user.login} · ${viewer.contributionsCollection.contributionCalendar.totalContributions} contributions in range`)}catch(e){githubVerified=false;state('githubVerifyState','err','GitHub verification failed: '+e.message)}}
async function testNetwork(){const body={probeHost:q('networkHost').value.trim(),probePort:+q('networkPort').value,dnsHost:q('networkDns').value.trim()};if(!validHost(body.probeHost)||!validHost(body.dnsHost)||body.probePort<1||body.probePort>65535)return state('networkTestState','err','Enter valid host names and port.');state('networkTestState','busy','DeskMate is running one bounded DNS/TCP test…');try{const r=await getJson('/api/test/network',{method:'POST',headers:jsonHeaders,body:JSON.stringify(body)});networkVerified=true;state('networkTestState','ok',`DNS ${r.dnsMs} ms (${r.resolved}) · TCP ${r.tcpMs} ms`)}catch(e){networkVerified=false;state('networkTestState','err','Target test failed: '+e.message)}}
function radarBody(){return{lat:+q('radarLat').value,lon:+q('radarLon').value,rangeKm:+q('radarRange').value,pollSec:+q('radarPoll').value,source:q('radarSource').value,webhookUrl:q('radarWebhook').value.trim(),uiScale:+q('radarScale').value,minAltFt:+q('radarMinAlt').value,unitsMi:q('radarMiles').value==='1',showLabels:q('radarLabels').checked,showVectors:q('radarVectors').checked,showRimDots:q('radarRim').checked}}
async function testRadar(){const r=radarBody();if(!validLatLon(r.lat,r.lon))return state('radarTestState','err','Latitude or longitude is invalid.');if(r.source==='webhook'&&!/^https?:\/\//i.test(r.webhookUrl))return state('radarTestState','err','Webhook must be an HTTP or HTTPS URL.');state('radarTestState','busy','DeskMate is testing one radar request…');try{const result=await getJson('/api/test/radar',{method:'POST',headers:jsonHeaders,body:JSON.stringify({radar:r})});radarVerified=true;state('radarTestState','ok',`Valid response · ${result.aircraft} aircraft`)}catch(e){radarVerified=false;state('radarTestState','err','Radar test failed: '+e.message)}}
function validHost(v){return /^[a-z0-9.-]{1,253}$/i.test(v)&&!v.includes('..')}
function validLatLon(lat,lon){return Number.isFinite(lat)&&Number.isFinite(lon)&&lat>=-90&&lat<=90&&lon>=-180&&lon<=180}
function validateAll(){const errors=[];const n=(id,min,max)=>{const v=+q(id).value;return Number.isFinite(v)&&v>=min&&v<=max};if(q('mode').value==='carousel'&&!['carouselWeather','carouselNetwork','carouselRadar','carouselGithub'].some(id=>q(id).checked))errors.push('Select at least one carousel screen');if(!/^[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?$/i.test(q('hostname').value.trim()))errors.push('Hostname is invalid');if(!selectedLocation?.verified||!validLatLon(selectedLocation.latitude,selectedLocation.longitude))errors.push('Select a verified weather location');if(!n('weatherPoll',300,3600))errors.push('Weather interval must be 300–3600 seconds');if(weatherFingerprint()!==initial.weather&&!weatherVerified)errors.push('Verify changed weather location or key');if(!n('networkPort',1,65535)||!n('networkPoll',3,300))errors.push('Network port or interval is invalid');if(networkFingerprint()!==initial.network&&!networkVerified)errors.push('Test changed network targets');if(!validLatLon(+q('radarLat').value,+q('radarLon').value)||!n('radarRange',1,500)||!n('radarPoll',3,3600)||!n('radarMinAlt',0,60000))errors.push('Radar numeric settings are invalid');if(radarFingerprint()!==initial.radar&&!radarVerified)errors.push('Test changed radar source');if(!n('githubPoll',300,3600)||![1,3,6,12].includes(+q('githubRange').value))errors.push('GitHub range or interval is invalid');if(q('githubToken').value&&!githubVerified)errors.push('Verify the new GitHub token');return errors}
function allSettings(){const body={mode:q('mode').value,carouselSec:+q('carouselSec').value,carouselWeather:q('carouselWeather').checked,carouselNetwork:q('carouselNetwork').checked,carouselRadar:q('carouselRadar').checked,carouselGithub:q('carouselGithub').checked,rotation:+q('rotation').value,brightness:+q('brightness').value,hostname:q('hostname').value.trim(),clock:clockBody().clock,weather:{apiKey:q('weatherKey').value.trim(),city:selectedLocation?.name||'',country:selectedLocation?.country||'',lat:selectedLocation?.latitude??0,lon:selectedLocation?.longitude??0,timezone:selectedLocation?.timezone||'',timezoneAbbr:selectedLocation?.timezoneAbbreviation||'',utcOffsetSec:selectedLocation?.utcOffsetSeconds||0,locationVerified:!!selectedLocation?.verified,metric:q('weatherMetric').value==='1',pollSec:+q('weatherPoll').value},network:{probeHost:q('networkHost').value.trim(),probePort:+q('networkPort').value,dnsHost:q('networkDns').value.trim(),pollSec:+q('networkPoll').value},radar:radarBody(),github:{token:q('githubToken').value.trim(),login:q('githubToken').dataset.login||config.github?.login||'',rangeMonths:+q('githubRange').value,pollSec:+q('githubPoll').value}};if(q('ssid').value.trim())body.wifi=[{ssid:q('ssid').value.trim(),pass:q('password').value}];return body}
async function saveAll(){const errors=validateAll();if(errors.length){setSaveState('Validation required','var(--rose)');return toast(errors[0])}setSaveState('Saving…','var(--amber)');q('saveButton').disabled=true;try{const r=await getJson('/api/config',{method:'POST',headers:jsonHeaders,body:JSON.stringify(allSettings())});if(!r.ok)throw new Error(r.error||'save failed');setSaveState('Saved','var(--green)');toast(r.reboot?'Saved · rebooting':'Settings saved');q('weatherKey').value='';q('githubToken').value='';await load()}catch(e){setSaveState('Save failed','var(--rose)');toast(e.message)}finally{q('saveButton').disabled=false}}
async function load(){try{config=await getJson('/api/config',{cache:'no-store'});q('mode').value=config.mode||'weather';q('carouselSec').value=config.carouselSec||30;q('rotation').value=config.rotation||0;q('brightness').value=config.brightness??90;q('brightnessValue').textContent=q('brightness').value;['carouselWeather','carouselNetwork','carouselRadar','carouselGithub'].forEach(id=>q(id).checked=!!config[id]);const c=config.clock||{};q('timeFormat').value=c.use24Hour===false?'12':'24';q('nightEnabled').checked=!!c.nightEnabled;q('nightStart').value=c.nightStart||'22:00';q('nightEnd').value=c.nightEnd||'07:00';q('nightLevel').value=c.nightLevel??0;q('nightValue').textContent=q('nightLevel').value;const w=config.weather||{};selectedLocation={name:w.city||'',country:w.country||'',countryCode:'',latitude:+w.lat,longitude:+w.lon,timezone:w.timezone||c.tz||'UTC',timezoneAbbreviation:w.timezoneAbbr||c.tzAbbr||'',utcOffsetSeconds:+(w.utcOffsetSec??c.utcOffsetSec??0),verified:!!w.locationVerified};q('weatherSearch').value=[selectedLocation.name,selectedLocation.country].filter(Boolean).join(', ');q('weatherMetric').value=w.metric===false?'0':'1';q('weatherPoll').value=w.pollSec||600;secretState('weatherKeyState',w.apiKeySet);renderLocationState();weatherVerified=!!w.apiKeySet;state('weatherVerifyState',w.apiKeySet?'ok':'',w.apiKeySet?'Stored key and location are configured. Re-enter the key only when changing them.':'OpenWeather key required.');const n=config.network||{};q('networkHost').value=n.probeHost||'1.1.1.1';q('networkPort').value=n.probePort||443;q('networkDns').value=n.dnsHost||'github.com';q('networkPoll').value=n.pollSec||10;const r=config.radar||{};q('radarLat').value=r.lat??0;q('radarLon').value=r.lon??0;q('radarRange').value=r.rangeKm||20;q('radarPoll').value=r.pollSec||10;q('radarSource').value=r.source||'direct';q('radarWebhook').value=r.webhookUrl||'';q('radarScale').value=r.uiScale??1;q('radarMinAlt').value=r.minAltFt||0;q('radarMiles').value=r.unitsMi?'1':'0';q('radarLabels').checked=!!r.showLabels;q('radarVectors').checked=!!r.showVectors;q('radarRim').checked=!!r.showRimDots;const g=config.github||{};q('githubRange').value=g.rangeMonths||12;q('githubPoll').value=g.pollSec||900;q('githubToken').dataset.login=g.login||'';secretState('githubKeyState',g.tokenSet);githubVerified=!!g.tokenSet;state('githubVerifyState',g.tokenSet?'ok':'',g.tokenSet?`Stored token verified previously${g.login?' as @'+g.login:''}.`:'GitHub token required.');q('ssid').value=config.wifi?.[0]?.ssid||'';q('hostname').value=config.hostname||'deskmate';networkVerified=radarVerified=true;initial.weather=weatherFingerprint();initial.network=networkFingerprint();initial.radar=radarFingerprint();initial.github=githubFingerprint();setSaveState('Ready');await loadStatus()}catch(e){toast('Load failed: '+e.message)}}
async function loadStatus(){if(uploading)return;try{const s=await getJson('/api/status',{cache:'no-store'});q('status').textContent=`${s.fw} ${s.version}\n${s.ssid||'not connected'} · ${s.ip||'-'} · ${s.rssi} dBm\nheap ${s.heap} · max block ${s.maxblk}\ntime ${s.time||'waiting for NTP'} · ${s.tz||'UTC'}\nnight ${s.night?'active':s.nightHeld?'waiting for NTP':'off'}\npolls ${s.pollCompleted||0} ok · ${s.pollFailed||0} failed · ${s.pollCurrent||'idle'}\ncoalesced ${s.pollCoalesced||0} · deferred ${s.pollDeferrals||0}\njob ${s.pollLastMs||0} ms last · ${s.pollAverageMs||0} ms avg · credit ${s.pollCreditsMs||0}\nuptime ${s.uptime}s · ${s.reset||'-'}`}catch(e){q('status').textContent='Status unavailable'}}
q('saveButton').onclick=saveAll;q('locationSearchButton').onclick=searchLocations;q('verifyWeatherButton').onclick=verifyWeather;q('verifyGithubButton').onclick=verifyGithub;q('testNetworkButton').onclick=testNetwork;q('testRadarButton').onclick=testRadar;q('copyWeatherLocation').onclick=()=>{if(!selectedLocation)return toast('Resolve weather location first');q('radarLat').value=selectedLocation.latitude;q('radarLon').value=selectedLocation.longitude;markRadarChanged()};q('refreshButton').onclick=async()=>{await fetch('/api/refresh',{method:'POST'});toast('All selected sources marked due')};q('rebootButton').onclick=async()=>{await fetch('/api/reboot',{method:'POST'});toast('Rebooting')};q('scanButton').onclick=async()=>{q('wifiResults').innerHTML='<span class="mini">Scanning…</span>';try{const list=await getJson('/api/scan');q('wifiResults').innerHTML=list.map((x,i)=>`<button class="result" data-i="${i}"><span><strong>${x.ssid||'(hidden)'}</strong><small>${x.enc?'secured':'open'}</small></span><small>${x.rssi} dBm</small></button>`).join('');q('wifiResults').querySelectorAll('button').forEach(b=>b.onclick=()=>{q('ssid').value=list[+b.dataset.i].ssid;q('wifiResults').innerHTML=''})}catch(e){q('wifiResults').innerHTML='<span class="mini">Scan failed</span>'}};
q('mode').onchange=()=>auto({mode:q('mode').value});q('rotation').onchange=()=>auto({rotation:+q('rotation').value});q('carouselSec').onchange=()=>auto({carouselSec:+q('carouselSec').value});['carouselWeather','carouselNetwork','carouselRadar','carouselGithub'].forEach(id=>q(id).onchange=()=>{const selected=['carouselWeather','carouselNetwork','carouselRadar','carouselGithub'].filter(x=>q(x).checked);if(!selected.length){q(id).checked=true;return toast('At least one carousel screen must stay selected')}auto({[id]:q(id).checked})});q('brightness').oninput=()=>q('brightnessValue').textContent=q('brightness').value;q('brightness').onchange=()=>auto({brightness:+q('brightness').value});q('nightLevel').oninput=()=>q('nightValue').textContent=q('nightLevel').value;['timeFormat','nightEnabled','nightStart','nightEnd','nightLevel'].forEach(id=>q(id).onchange=()=>auto(clockBody()));q('weatherKey').addEventListener('input',markWeatherChanged);['networkHost','networkPort','networkDns'].forEach(id=>q(id).addEventListener('input',markNetworkChanged));['radarLat','radarLon','radarSource','radarWebhook','radarRange'].forEach(id=>q(id).addEventListener('change',markRadarChanged));q('githubToken').oninput=markGithubChanged;
function uploadUi(kind,text,pct=0){uploading=kind==='busy'||kind==='waiting';q('uploadButton').disabled=uploading||!q('firmware').files[0];q('firmware').disabled=uploading;q('saveButton').disabled=uploading;q('uploadButton').textContent=kind==='waiting'?'Waiting for reboot…':kind==='busy'?'Uploading…':'Upload firmware';q('uploadProgress').style.width=pct+'%';q('uploadStatus').className='upload-status '+(kind==='ok'?'ok':kind==='err'?'err':'');q('uploadStatus').textContent=text}
q('firmware').onchange=async()=>{const f=q('firmware').files[0];if(!f)return uploadUi('idle','Choose a .bin file to begin.',0);if(!f.name.toLowerCase().endsWith('.bin')){q('firmware').value='';return uploadUi('err','Only .bin files are accepted.',0)}const first=new Uint8Array(await f.slice(0,1).arrayBuffer());if(first[0]!==0xE9){q('firmware').value='';return uploadUi('err','This does not look like an ESP firmware image (missing 0xE9 header).',0)}uploadUi('idle',`${f.name} · ${(f.size/1024).toFixed(1)} KiB · header verified`,0)};
function waitForDevice(){let tries=0;uploadUi('waiting','Uploaded. DeskMate is rebooting…',100);const timer=setInterval(async()=>{tries++;try{const r=await fetch('/api/status',{cache:'no-store'});if(r.ok){clearInterval(timer);uploading=false;q('firmware').value='';q('firmware').disabled=false;q('saveButton').disabled=false;uploadUi('ok','Update complete. DeskMate is online.',100);load()}}catch(e){}if(tries>35){clearInterval(timer);uploading=false;q('firmware').disabled=false;q('saveButton').disabled=false;uploadUi('ok','Upload completed. Reconnect manually if this page does not recover.',100)}},2000)}
q('uploadButton').onclick=()=>{const f=q('firmware').files[0];if(!f)return;const data=new FormData;data.append('update',f,f.name);const xhr=new XMLHttpRequest;let sent=false;xhr.open('POST','/update');xhr.timeout=180000;uploadUi('busy','Preparing upload…',1);xhr.upload.onprogress=e=>{if(e.lengthComputable){sent=e.loaded>=e.total;const p=Math.max(1,Math.min(99,Math.round(e.loaded/e.total*100)));uploadUi('busy',`Uploading ${p}% · ${(e.loaded/1024).toFixed(0)} / ${(e.total/1024).toFixed(0)} KiB`,p)}};xhr.onload=()=>xhr.status>=200&&xhr.status<300?waitForDevice():uploadUi('err','Upload failed: '+(xhr.responseText||xhr.status),0);xhr.onerror=()=>sent?waitForDevice():uploadUi('err','Connection failed during upload.',0);xhr.ontimeout=()=>sent?waitForDevice():uploadUi('err','Upload timed out.',0);xhr.send(data)};
window.addEventListener('beforeunload',e=>{if(uploading){e.preventDefault();e.returnValue='Upload is still running.'}});load();setInterval(loadStatus,10000);
</script></body></html>)HTML";
